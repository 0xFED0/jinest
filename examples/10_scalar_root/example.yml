# A valid document does not have to be a mapping or list.
# Jinest returns this JSON-valid scalar unchanged. Native YAML date/datetime
# and bytes roots are accepted too; example 09 shows their output formats.
42
