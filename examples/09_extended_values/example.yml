# YAML SafeLoader creates Python date/datetime/bytes values for these scalars.
#
# YAML result:
#   python jinest.py examples/09_extended_values/example.yml --output-format yaml
# JSON result:
#   python jinest.py examples/09_extended_values/example.yml --output-format json
# `payload` becomes a lossless Latin-1 JSON string; after json.loads(),
# use payload.encode("latin-1") when the receiving schema declares it binary.

calendar_date: 2026-08-02
timestamp: 2026-08-02T03:04:05+00:00
payload: !!binary AP9B
