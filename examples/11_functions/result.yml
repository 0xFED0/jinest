strings: {}
defaults:
  factor: 10
helpers:
  prefix: helper-
native_result: 25
text_result: '"25"'
script_result: 10
structured_result:
  value: hello
  type: generated
# A list structural body materializes like any other returned node.
list_structural_result:
- left
- right
# Both attachments have their own destination paths and nested caches.
rebound_nodes:
  x:
    value: 10
    where: global_root.rebound_nodes.x
    nested:
      where: global_root.rebound_nodes.x.nested
  y:
    value: 10
    where: global_root.rebound_nodes.y
    nested:
      where: global_root.rebound_nodes.y.nested
  temporary_where: global_root.rebound_nodes
deployments:
  host: api.example.test
  primary:
    name: jinest
    address: api.example.test:8080
    path: global_root.deployments.primary
    label: JINEST
scaled_result: 50
namespace_result: <HELLO>
output:
  prefix: output-
  formatted: output-name

